const { getDatabase } = require("../database/sqlite");
function getAllBusinessLocations(clientId){return getDatabase().prepare("SELECT * FROM business_locations WHERE client_id=? ORDER BY created_at DESC").all(clientId)}
function createBusinessLocation(d){const db=getDatabase();db.transaction(()=>{if(d.is_primary)db.prepare("UPDATE business_locations SET is_primary=0 WHERE client_id=?").run(d.client_id);db.prepare(`INSERT INTO business_locations(id,client_id,name,code,gst_number,address,city,state,country,postal_code,phone,email,is_primary,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,CURRENT_TIMESTAMP,CURRENT_TIMESTAMP)`).run(d.id,d.client_id,d.name,d.code,d.gst_number||null,d.address,d.city,d.state,d.country,d.postal_code||null,d.phone||null,d.email||null,d.is_primary?1:0)})();return{success:true,id:d.id}}
function updateBusinessLocation(d){const db=getDatabase();db.transaction(()=>{if(d.is_primary)db.prepare("UPDATE business_locations SET is_primary=0 WHERE client_id=?").run(d.client_id);db.prepare(`UPDATE business_locations SET name=?,code=?,gst_number=?,address=?,city=?,state=?,country=?,postal_code=?,phone=?,email=?,is_primary=?,updated_at=CURRENT_TIMESTAMP WHERE id=? AND client_id=?`).run(d.name,d.code,d.gst_number||null,d.address,d.city,d.state,d.country,d.postal_code||null,d.phone||null,d.email||null,d.is_primary?1:0,d.id,d.client_id)})();return{success:true}}
function deleteBusinessLocation(id,clientId){getDatabase().prepare("DELETE FROM business_locations WHERE id=? AND client_id=?").run(id,clientId);return{success:true}}
module.exports={getAllBusinessLocations,createBusinessLocation,updateBusinessLocation,deleteBusinessLocation};
