const { ipcMain }=require("electron");const r=require("../repositories/units.repository");function registerUnitsIpc(){ipcMain.handle("units:getAll",(_,id)=>r.getAllUnits(id));ipcMain.handle("units:create",(_,d)=>r.createUnit(d));ipcMain.handle("units:update",(_,d)=>r.updateUnit(d));ipcMain.handle("units:delete",(_,x)=>r.deleteUnit(x.id,x.clientId))}module.exports={registerUnitsIpc};
